{
  "name": "credit-card-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Credit Card Deals Tracker - Aggregate and track credit card welcome offers",
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "start": "cd apps/web && npm start",
    "db:setup": "cd packages/database && npm run db:generate && npm run db:push && npm run db:seed"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
